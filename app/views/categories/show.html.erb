<%= render 'shared/breadcrumb', category: @category %>
<h1><%= @category.name %></h1>

<section id="posts">
  <%= render @posts %>
</section>
<div class="d-flex justify-content-center mt-5"><%= paginate @posts %></div>
