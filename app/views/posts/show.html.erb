<h1><%= @post.title %></h1>

<small>
  <time><%= @post.created_at.to_date.strftime("%d %B, %Y")%></time>
  |
  <%= link_to t('posts.post.edit'), edit_post_path(@post), class: "text-warning"%>
  |
  <%= link_to t('posts.post.delete'),  post_path(@post), class: "text-danger", method: :delete, data: {confirm: 'Are your sure?'}%>
</small>

<p><%= @post.body %></p>
